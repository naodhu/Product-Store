{
  "name": "mern-auth-boilerplate",
  "version": "1.0.0",
  "description": "this is the default description for a new project",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index",
    "client": "npm run start --prefix client",
    "build": "cd client && npm install && npm run build",
    "server": "node server/index.js",
    "app": "concurrently npm \"npm run server\" \"npm run client\""
  },
 
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^7.6.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.9.0"
  }
}
